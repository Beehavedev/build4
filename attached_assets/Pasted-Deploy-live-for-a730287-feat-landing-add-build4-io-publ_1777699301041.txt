Deploy live for a730287: feat(landing): add build4.io public landing page with WalletConnect "Launch App" The repo previously had no landing page — `/` returned 404 and the only HTML served was the Telegram mini-app at `/app`. This adds a real public landing page at `https://build4.io/` so the marketing domain has something to show. Page (public/index.html): - Single self-contained HTML file (no Vite, no npm dep, no build step) - Dark BUILD4 brand: bg #0D0D0F, accent #8B5CF6, soft purple glow - Hero with gradient headline + lede - Primary CTA: "Launch App" (WalletConnect) - Secondary CTA: "Open on Telegram" (direct deep link, always works) - Status row that surfaces success / cancellation / errors clearly - Venue strip: Hyperliquid, Aster, Polymarket, 42.space - SEO meta + Open Graph tags Wallet flow: - @walletconnect/ethereum-provider@2.17.0 from esm.sh CDN (avoids editing package.json, keeps page weight off the critical path — provider only loads on first click) - Chain: BSC mainnet (56) — matches BUILD4 builder/treasury, native Trust Wallet support - WalletConnect modal themed dark with purple accent so the connect flow doesn't look jarring against the page - Already-connected sessions skip straight to the Telegram redirect - User-cancellation ("rejected", "closed", "expired") is treated as a soft cancel, not a red error - On successful connect → redirect to https://t.me/BUILD4_BOT Server (src/server.ts): - New `GET /` route serves public/index.html - WALLETCONNECT_PROJECT_ID is read from env and substituted into a `__WALLETCONNECT_PROJECT_ID__` placeholder at first read, then the rendered HTML is cached in memory - Missing project id is non-fatal: page still renders, button shows a clear error when clicked - Fallback: if the file is missing on disk, serve a meta-refresh to the Telegram bot rather than 500 - Cache-Control: public, max-age=300 (short — picks up fixes within minutes) No changes to package.json, the mini-app build, or any existing route.
May 2, 2026 at 9:17 AM