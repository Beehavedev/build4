CREATE TABLE IF NOT EXISTS "agent_audit_logs" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, action_type TEXT NOT NULL, target_agent_id VARCHAR, details_json TEXT, result TEXT DEFAULT 'success'::text NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_collaboration_log" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, requesting_agent_id VARCHAR NOT NULL, consulted_agent_id VARCHAR NOT NULL, question TEXT NOT NULL, answer TEXT NOT NULL, used_in_context TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_constitution" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, law_number INTEGER NOT NULL, law_title TEXT NOT NULL, law_text TEXT NOT NULL, is_immutable BOOLEAN DEFAULT true NOT NULL, version INTEGER DEFAULT 1 NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_conversation_memory" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, twitter_username TEXT NOT NULL, last_interaction TEXT, sentiment TEXT DEFAULT 'neutral'::text NOT NULL, interaction_count INTEGER DEFAULT 1 NOT NULL, last_interaction_at TIMESTAMP DEFAULT now(), created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_evolutions" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, from_model TEXT, to_model TEXT NOT NULL, reason TEXT, metrics_json TEXT, verification_hash TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_jobs" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, client_agent_id VARCHAR NOT NULL, worker_agent_id VARCHAR, title TEXT NOT NULL, description TEXT NOT NULL, category TEXT DEFAULT 'general'::text NOT NULL, budget TEXT NOT NULL, status TEXT DEFAULT 'open'::text NOT NULL, result_json TEXT, escrow_amount TEXT, completed_at TIMESTAMP, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_knowledge_base" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, title TEXT NOT NULL, content TEXT NOT NULL, source_type TEXT DEFAULT 'custom'::text NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_lineage" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, parent_agent_id VARCHAR NOT NULL, child_agent_id VARCHAR NOT NULL, revenue_share_bps INTEGER DEFAULT 1000 NOT NULL, total_revenue_shared TEXT DEFAULT '0'::text NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_memory" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, memory_type TEXT NOT NULL, key TEXT NOT NULL, value TEXT NOT NULL, confidence INTEGER DEFAULT 50 NOT NULL, updated_at TIMESTAMP DEFAULT now(), created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_messages" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, from_agent_id VARCHAR NOT NULL, to_agent_id VARCHAR NOT NULL, subject TEXT, body TEXT NOT NULL, status TEXT DEFAULT 'unread'::text NOT NULL, read_at TIMESTAMP, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_runtime_profiles" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, model_name TEXT DEFAULT 'meta-llama/Llama-3.1-70B-Instruct'::text NOT NULL, model_version TEXT, config_json TEXT, created_at TIMESTAMP DEFAULT now(), updated_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_skill_configs" (id INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, chat_id TEXT NOT NULL, skill_id TEXT NOT NULL, enabled BOOLEAN DEFAULT true NOT NULL, config TEXT DEFAULT '{}'::text NOT NULL, updated_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_skills" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, name TEXT NOT NULL, description TEXT, price_amount TEXT DEFAULT '0'::text NOT NULL, category TEXT DEFAULT 'general'::text NOT NULL, is_active BOOLEAN DEFAULT true NOT NULL, total_purchases INTEGER DEFAULT 0 NOT NULL, total_revenue TEXT DEFAULT '0'::text NOT NULL, created_at TIMESTAMP DEFAULT now(), code TEXT, input_schema TEXT, output_schema TEXT, example_input TEXT, example_output TEXT, version INTEGER DEFAULT 1 NOT NULL, is_executable BOOLEAN DEFAULT false NOT NULL, execution_count INTEGER DEFAULT 0 NOT NULL, avg_rating INTEGER DEFAULT 0 NOT NULL, total_ratings INTEGER DEFAULT 0 NOT NULL, tier TEXT DEFAULT 'bronze'::text NOT NULL, total_royalties TEXT DEFAULT '0'::text NOT NULL);
CREATE TABLE IF NOT EXISTS "agent_soul_entries" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, entry TEXT NOT NULL, entry_type TEXT DEFAULT 'reflection'::text NOT NULL, source TEXT DEFAULT 'self'::text NOT NULL, metadata TEXT, integrity_hash TEXT, previous_hash TEXT, created_at TIMESTAMP DEFAULT now());
ALTER TABLE "agent_soul_entries" ADD COLUMN IF NOT EXISTS integrity_hash TEXT;
ALTER TABLE "agent_soul_entries" ADD COLUMN IF NOT EXISTS previous_hash TEXT;
ALTER TABLE "agent_soul_entries" ADD COLUMN IF NOT EXISTS ipfs_cid TEXT;
ALTER TABLE "agent_soul_entries" ADD COLUMN IF NOT EXISTS anchor_tx_hash TEXT;
ALTER TABLE "agent_soul_entries" ADD COLUMN IF NOT EXISTS anchor_chain_id INTEGER;
CREATE TABLE IF NOT EXISTS "agent_strategy_memos" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, memo_type TEXT NOT NULL, title TEXT NOT NULL, content TEXT NOT NULL, summary TEXT NOT NULL, metrics TEXT, status TEXT DEFAULT 'active'::text NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_subscriptions" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, wallet_address TEXT NOT NULL, plan_id VARCHAR NOT NULL, status TEXT DEFAULT 'active'::text NOT NULL, started_at TIMESTAMP DEFAULT now(), expires_at TIMESTAMP, tx_hash TEXT, chain_id INTEGER, inference_used INTEGER DEFAULT 0 NOT NULL, skill_executions_used INTEGER DEFAULT 0 NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_survival_status" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, tier TEXT DEFAULT 'normal'::text NOT NULL, previous_tier TEXT, last_transition_at TIMESTAMP DEFAULT now(), reason TEXT, turns_alive INTEGER DEFAULT 0 NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_tasks" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, creator_wallet VARCHAR, task_type TEXT NOT NULL, title TEXT NOT NULL, description TEXT NOT NULL, status TEXT DEFAULT 'pending'::text NOT NULL, result TEXT, tools_used TEXT, model_used TEXT, execution_time_ms INTEGER, created_at TIMESTAMP DEFAULT now(), completed_at TIMESTAMP);
CREATE TABLE IF NOT EXISTS "agent_tool_results" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, tool_type TEXT NOT NULL, result TEXT NOT NULL, used_in_tweet_id TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agent_transactions" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, type TEXT NOT NULL, amount TEXT NOT NULL, counterparty_agent_id VARCHAR, reference_type TEXT, reference_id VARCHAR, description TEXT, created_at TIMESTAMP DEFAULT now(), tx_hash TEXT, chain_id INTEGER);
CREATE TABLE IF NOT EXISTS "agent_twitter_accounts" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, twitter_handle TEXT NOT NULL, twitter_api_key TEXT NOT NULL, twitter_api_secret TEXT NOT NULL, twitter_access_token TEXT NOT NULL, twitter_access_token_secret TEXT NOT NULL, role TEXT DEFAULT 'cmo'::text NOT NULL, enabled INTEGER DEFAULT 0 NOT NULL, personality TEXT DEFAULT ''::text, instructions TEXT DEFAULT ''::text, posting_frequency_mins INTEGER DEFAULT 60, auto_reply_enabled INTEGER DEFAULT 1 NOT NULL, auto_bounty_enabled INTEGER DEFAULT 0 NOT NULL, default_reward_bnb TEXT DEFAULT '0.015'::text, last_posted_at TIMESTAMP, last_mention_id TEXT, replied_tweet_ids TEXT DEFAULT ''::text, total_tweets INTEGER DEFAULT 0 NOT NULL, total_replies INTEGER DEFAULT 0 NOT NULL, total_bounties INTEGER DEFAULT 0 NOT NULL, created_at TIMESTAMP DEFAULT now(), updated_at TIMESTAMP DEFAULT now(), company_name TEXT DEFAULT ''::text, company_description TEXT DEFAULT ''::text, company_product TEXT DEFAULT ''::text, company_audience TEXT DEFAULT ''::text, company_website TEXT DEFAULT ''::text, company_key_messages TEXT DEFAULT ''::text, owner_telegram_chat_id TEXT, preferred_model TEXT);
CREATE TABLE IF NOT EXISTS "agent_wallets" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, balance TEXT DEFAULT '0'::text NOT NULL, total_earned TEXT DEFAULT '0'::text NOT NULL, total_spent TEXT DEFAULT '0'::text NOT NULL, status TEXT DEFAULT 'active'::text NOT NULL, last_active_at TIMESTAMP DEFAULT now(), created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "agents" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, name TEXT NOT NULL, bio TEXT, model_type TEXT DEFAULT 'meta-llama/Llama-3.1-70B-Instruct'::text NOT NULL, status TEXT DEFAULT 'active'::text NOT NULL, created_at TIMESTAMP DEFAULT now(), onchain_id TEXT, onchain_registered BOOLEAN DEFAULT false NOT NULL, creator_wallet TEXT, preferred_chain TEXT DEFAULT 'bnbMainnet'::text, erc8004_registered BOOLEAN DEFAULT false NOT NULL, bap578_registered BOOLEAN DEFAULT false NOT NULL);
CREATE TABLE IF NOT EXISTS "api_keys" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, wallet_address TEXT NOT NULL, key_hash TEXT NOT NULL, key_prefix TEXT NOT NULL, label TEXT DEFAULT 'default'::text NOT NULL, status TEXT DEFAULT 'active'::text NOT NULL, total_requests INTEGER DEFAULT 0 NOT NULL, total_tokens INTEGER DEFAULT 0 NOT NULL, total_spent TEXT DEFAULT '0'::text NOT NULL, rate_limit INTEGER DEFAULT 60 NOT NULL, last_used_at TIMESTAMP, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "api_usage" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, api_key_id VARCHAR NOT NULL, wallet_address TEXT NOT NULL, model TEXT NOT NULL, provider TEXT NOT NULL, tokens_used INTEGER DEFAULT 0 NOT NULL, cost_amount TEXT DEFAULT '0'::text NOT NULL, latency_ms INTEGER, status TEXT DEFAULT 'success'::text NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "aster_credentials" (chat_id TEXT NOT NULL, encrypted_api_key TEXT NOT NULL, encrypted_api_secret TEXT NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "bap578_nfas" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR, token_id TEXT, chain_id TEXT DEFAULT '56'::text NOT NULL, owner_wallet TEXT NOT NULL, name TEXT NOT NULL, description TEXT, logic_address TEXT, metadata_uri TEXT, learning_root TEXT, learning_mode TEXT DEFAULT 'json'::text NOT NULL, status TEXT DEFAULT 'active'::text NOT NULL, template_id TEXT, vault_permissions TEXT, created_at TIMESTAMP DEFAULT now(), tx_hash TEXT, contract_address TEXT, personality_profile TEXT, personality_hash TEXT, traits TEXT, voice TEXT, values_text TEXT, behavior_rules TEXT, communication_style TEXT);
CREATE TABLE IF NOT EXISTS "bounty_activity_feed" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, event_type TEXT NOT NULL, agent_name TEXT NOT NULL, agent_id TEXT, bounty_id TEXT, bounty_title TEXT, amount TEXT, worker_wallet TEXT, worker_agent_id TEXT, message TEXT NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "bounty_submissions" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, job_id VARCHAR NOT NULL, worker_agent_id VARCHAR NOT NULL, worker_wallet TEXT, result_json TEXT, status TEXT DEFAULT 'submitted'::text NOT NULL, rating INTEGER, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "chaos_milestones" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, launch_id VARCHAR NOT NULL, milestone_number INTEGER NOT NULL, name TEXT NOT NULL, description TEXT NOT NULL, action TEXT NOT NULL, trigger_after_minutes INTEGER NOT NULL, status TEXT DEFAULT 'pending'::text NOT NULL, tx_hash TEXT, tweet_id TEXT, tweet_text TEXT, tokens_burned TEXT, tokens_transferred TEXT, executed_at TIMESTAMP, error_message TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "data_listings" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, wallet_address TEXT, name TEXT NOT NULL, description TEXT, category TEXT DEFAULT 'general'::text NOT NULL, price_amount TEXT DEFAULT '0'::text NOT NULL, data_type TEXT DEFAULT 'dataset'::text NOT NULL, data_format TEXT DEFAULT 'json'::text NOT NULL, data_size TEXT, sample_data TEXT, content_hash TEXT, total_sales INTEGER DEFAULT 0 NOT NULL, total_revenue TEXT DEFAULT '0'::text NOT NULL, is_active BOOLEAN DEFAULT true NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "data_purchases" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, listing_id VARCHAR NOT NULL, buyer_wallet TEXT NOT NULL, buyer_agent_id VARCHAR, seller_agent_id VARCHAR NOT NULL, amount TEXT NOT NULL, platform_fee TEXT DEFAULT '0'::text NOT NULL, status TEXT DEFAULT 'completed'::text NOT NULL, tx_hash TEXT, chain_id INTEGER, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "erc8004_identities" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR, agent_registry TEXT, chain_id TEXT DEFAULT '56'::text NOT NULL, agent_uri TEXT, owner_wallet TEXT NOT NULL, name TEXT NOT NULL, description TEXT, image TEXT, services_json TEXT, supported_trust TEXT, active BOOLEAN DEFAULT true NOT NULL, created_at TIMESTAMP DEFAULT now(), onchain_token_id TEXT, tx_hash TEXT, registry_address TEXT);
CREATE TABLE IF NOT EXISTS "erc8004_reputation" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_identity_id VARCHAR NOT NULL, client_wallet TEXT NOT NULL, value INTEGER NOT NULL, value_decimals INTEGER DEFAULT 0 NOT NULL, tag1 TEXT, tag2 TEXT, endpoint TEXT, feedback_uri TEXT, feedback_hash TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "erc8004_validations" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_identity_id VARCHAR NOT NULL, validator_wallet TEXT NOT NULL, method TEXT DEFAULT 'reputation'::text NOT NULL, result TEXT DEFAULT 'pass'::text NOT NULL, score INTEGER, proof_uri TEXT, proof_hash TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "inference_providers" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, name TEXT NOT NULL, type TEXT DEFAULT 'centralized'::text NOT NULL, network TEXT, models_supported ARRAY, cost_per_request TEXT DEFAULT '0'::text NOT NULL, latency_ms INTEGER, is_active BOOLEAN DEFAULT true NOT NULL, verifiable BOOLEAN DEFAULT false NOT NULL, decentralized BOOLEAN DEFAULT false NOT NULL, metadata TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "inference_requests" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, provider_id VARCHAR NOT NULL, model TEXT NOT NULL, prompt TEXT NOT NULL, response TEXT, status TEXT DEFAULT 'pending'::text NOT NULL, cost_amount TEXT DEFAULT '0'::text NOT NULL, latency_ms INTEGER, proof_hash TEXT, proof_type TEXT, proof_anchored BOOLEAN DEFAULT false NOT NULL, prefer_decentralized BOOLEAN DEFAULT true NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "outreach_campaigns" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, type TEXT NOT NULL, status TEXT DEFAULT 'pending'::text NOT NULL, targets_sent INTEGER DEFAULT 0 NOT NULL, targets_reached INTEGER DEFAULT 0 NOT NULL, targets_failed INTEGER DEFAULT 0 NOT NULL, beacon_tx_hashes ARRAY, message TEXT, started_at TIMESTAMP, completed_at TIMESTAMP, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "outreach_targets" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, platform TEXT NOT NULL, name TEXT NOT NULL, endpoint_url TEXT, discovery_url TEXT, chain_id INTEGER, contract_address TEXT, method TEXT DEFAULT 'http'::text NOT NULL, status TEXT DEFAULT 'pending'::text NOT NULL, last_contacted_at TIMESTAMP, last_response TEXT, response_code INTEGER, times_contacted INTEGER DEFAULT 0 NOT NULL, discovered BOOLEAN DEFAULT false NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "platform_revenue" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, fee_type TEXT NOT NULL, amount TEXT NOT NULL, agent_id VARCHAR, reference_id VARCHAR, description TEXT, tx_hash TEXT, chain_id INTEGER, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "privacy_transfers" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, chain_id INTEGER NOT NULL, token_symbol TEXT NOT NULL, token_address TEXT NOT NULL, burn_address TEXT NOT NULL, recipient_address TEXT NOT NULL, amount TEXT NOT NULL, status TEXT DEFAULT 'pending'::text NOT NULL, deposit_tx_hash TEXT, withdrawal_tx_hash TEXT, proof_id TEXT, secret_hint TEXT, error_message TEXT, created_at TIMESTAMP DEFAULT now(), completed_at TIMESTAMP);
CREATE TABLE IF NOT EXISTS "skill_executions" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, skill_id VARCHAR NOT NULL, caller_type TEXT DEFAULT 'user'::text NOT NULL, caller_id VARCHAR, input_json TEXT, output_json TEXT, status TEXT DEFAULT 'pending'::text NOT NULL, error_message TEXT, latency_ms INTEGER, rating INTEGER, cost_wei TEXT DEFAULT '0'::text NOT NULL, tx_hash TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "skill_pipelines" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, name TEXT NOT NULL, description TEXT, creator_agent_id VARCHAR NOT NULL, skill_ids ARRAY NOT NULL, price_amount TEXT DEFAULT '0'::text NOT NULL, execution_count INTEGER DEFAULT 0 NOT NULL, total_royalties TEXT DEFAULT '0'::text NOT NULL, tier TEXT DEFAULT 'bronze'::text NOT NULL, is_active BOOLEAN DEFAULT true NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "skill_purchases" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, skill_id VARCHAR NOT NULL, buyer_agent_id VARCHAR NOT NULL, seller_agent_id VARCHAR NOT NULL, amount TEXT NOT NULL, status TEXT DEFAULT 'pending'::text NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "strategy_action_items" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, memo_id VARCHAR NOT NULL, action TEXT NOT NULL, priority TEXT DEFAULT 'medium'::text NOT NULL, status TEXT DEFAULT 'pending'::text NOT NULL, completed_at TIMESTAMP, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "subscription_plans" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, name TEXT NOT NULL, tier TEXT DEFAULT 'free'::text NOT NULL, price_amount TEXT DEFAULT '0'::text NOT NULL, currency TEXT DEFAULT 'BNB'::text NOT NULL, duration_days INTEGER DEFAULT 30 NOT NULL, inference_limit INTEGER DEFAULT 100 NOT NULL, skill_execution_limit INTEGER DEFAULT 50 NOT NULL, agent_slots INTEGER DEFAULT 1 NOT NULL, data_listing_limit INTEGER DEFAULT 5 NOT NULL, api_rate_limit INTEGER DEFAULT 60 NOT NULL, priority_support BOOLEAN DEFAULT false NOT NULL, is_active BOOLEAN DEFAULT true NOT NULL, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "support_agent_config" (id VARCHAR DEFAULT 'default'::character varying NOT NULL, enabled INTEGER DEFAULT 0, polling_interval_ms INTEGER DEFAULT 120000, last_mention_id TEXT, replied_tweet_ids TEXT, updated_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "support_tickets" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, tweet_id TEXT NOT NULL, tweet_url TEXT, twitter_handle TEXT NOT NULL, twitter_user_id TEXT, user_message TEXT NOT NULL, category TEXT DEFAULT 'general'::text NOT NULL, priority TEXT DEFAULT 'normal'::text NOT NULL, ai_summary TEXT, ai_reply_text TEXT, reply_tweet_id TEXT, status TEXT DEFAULT 'open'::text NOT NULL, resolution TEXT, created_at TIMESTAMP DEFAULT now(), resolved_at TIMESTAMP);
CREATE TABLE IF NOT EXISTS "telegram_wallets" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, chat_id TEXT NOT NULL, wallet_address TEXT NOT NULL, is_active BOOLEAN DEFAULT true NOT NULL, created_at TIMESTAMP DEFAULT now(), encrypted_private_key TEXT);
CREATE TABLE IF NOT EXISTS "token_launches" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR, creator_wallet TEXT, platform TEXT NOT NULL, chain_id INTEGER NOT NULL, token_name TEXT NOT NULL, token_symbol TEXT NOT NULL, token_description TEXT, image_url TEXT, token_address TEXT, tx_hash TEXT, launch_url TEXT, initial_liquidity_bnb TEXT, status TEXT DEFAULT 'pending'::text NOT NULL, error_message TEXT, metadata TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "trade_outcomes" (id INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, chat_id TEXT NOT NULL, token_address TEXT NOT NULL, token_symbol TEXT NOT NULL, result TEXT NOT NULL, pnl_bnb DOUBLE PRECISION NOT NULL, peak_multiple DOUBLE PRECISION DEFAULT 1 NOT NULL, entry_price_bnb DOUBLE PRECISION NOT NULL, hold_time_minutes INTEGER DEFAULT 0 NOT NULL, confidence_score INTEGER DEFAULT 50 NOT NULL, source TEXT DEFAULT 'ai_scan'::text NOT NULL, entry_progress_percent DOUBLE PRECISION DEFAULT 0, entry_age_minutes INTEGER DEFAULT 0, entry_velocity DOUBLE PRECISION DEFAULT 0, entry_holder_count INTEGER DEFAULT 0, entry_raised_bnb DOUBLE PRECISION DEFAULT 0, entry_rug_risk INTEGER DEFAULT 0, reasoning TEXT, hour_of_day INTEGER, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "trading_preferences" (chat_id TEXT NOT NULL, enabled BOOLEAN DEFAULT false NOT NULL, buy_amount_bnb TEXT DEFAULT '0.1'::text NOT NULL, take_profit_multiple DOUBLE PRECISION DEFAULT 2 NOT NULL, stop_loss_multiple DOUBLE PRECISION DEFAULT 0.7 NOT NULL, max_positions INTEGER DEFAULT 5 NOT NULL, updated_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "tweet_performance" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, agent_id VARCHAR NOT NULL, tweet_id TEXT, tweet_text TEXT NOT NULL, strategy_memo_id VARCHAR, theme_alignment INTEGER, aligned_themes TEXT, engagement_score INTEGER, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "twitter_agent_config" (id VARCHAR DEFAULT 'default'::character varying NOT NULL, enabled INTEGER DEFAULT 0, polling_interval_ms INTEGER DEFAULT 300000, min_verification_score INTEGER DEFAULT 60, max_payout_bnb TEXT DEFAULT '0.015'::text, default_bounty_budget TEXT DEFAULT '0.015'::text, agent_id VARCHAR, updated_at TIMESTAMP DEFAULT now(), max_winners_per_bounty INTEGER DEFAULT 3, last_mention_id TEXT, replied_tweet_ids TEXT);
CREATE TABLE IF NOT EXISTS "twitter_agent_personality" (id VARCHAR DEFAULT 'default'::character varying NOT NULL, voice TEXT DEFAULT ''::text, values TEXT DEFAULT ''::text, do_list TEXT DEFAULT ''::text, dont_list TEXT DEFAULT ''::text, learned_lessons TEXT DEFAULT ''::text, top_performing_styles TEXT DEFAULT ''::text, reflection_count INTEGER DEFAULT 0, last_reflection_at TIMESTAMP, updated_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "twitter_bounties" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, job_id VARCHAR NOT NULL, tweet_id TEXT, tweet_url TEXT, tweet_text TEXT, status TEXT DEFAULT 'pending'::text NOT NULL, replies_checked INTEGER DEFAULT 0, last_checked_at TIMESTAMP, since_id TEXT, error_message TEXT, created_at TIMESTAMP DEFAULT now(), reward_bnb TEXT DEFAULT '0.015'::text, max_winners INTEGER DEFAULT 3, winners_count INTEGER DEFAULT 0);
CREATE TABLE IF NOT EXISTS "twitter_reply_log" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, tweet_id TEXT, in_reply_to_user TEXT, in_reply_to_text TEXT, reply_text TEXT, tone TEXT, engagement INTEGER DEFAULT 0, likes INTEGER DEFAULT 0, retweets INTEGER DEFAULT 0, replies INTEGER DEFAULT 0, self_score INTEGER DEFAULT 0, reflection_notes TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "twitter_submissions" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, twitter_bounty_id VARCHAR NOT NULL, job_id VARCHAR NOT NULL, twitter_user_id TEXT NOT NULL, twitter_handle TEXT NOT NULL, tweet_id TEXT NOT NULL, tweet_text TEXT NOT NULL, wallet_address TEXT, proof_summary TEXT, verification_score INTEGER, verification_reason TEXT, status TEXT DEFAULT 'pending'::text NOT NULL, payment_tx_hash TEXT, payment_amount TEXT, reply_tweet_id TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "user_credits" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, session_id TEXT NOT NULL, free_executions_used INTEGER DEFAULT 0 NOT NULL, wallet_address TEXT, total_paid TEXT DEFAULT '0'::text NOT NULL, created_at TIMESTAMP DEFAULT now(), updated_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "telegram_bot_referrals" (id VARCHAR DEFAULT gen_random_uuid() PRIMARY KEY, referrer_chat_id TEXT NOT NULL, referred_chat_id TEXT NOT NULL, referral_code TEXT NOT NULL, status TEXT DEFAULT 'pending'::text NOT NULL, commission_percent INTEGER DEFAULT 30 NOT NULL, commission_amount TEXT, commission_paid BOOLEAN DEFAULT false NOT NULL, commission_tx_hash TEXT, created_at TIMESTAMP DEFAULT now(), paid_at TIMESTAMP);
CREATE TABLE IF NOT EXISTS "telegram_bot_subscriptions" (id VARCHAR DEFAULT gen_random_uuid() PRIMARY KEY, wallet_address TEXT NOT NULL, chat_id TEXT NOT NULL, status TEXT DEFAULT 'trial'::text NOT NULL, trial_started_at TIMESTAMP DEFAULT now(), paid_at TIMESTAMP, expires_at TIMESTAMP, tx_hash TEXT, chain_id INTEGER, amount_paid TEXT, created_at TIMESTAMP DEFAULT now());
CREATE TABLE IF NOT EXISTS "users" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, username TEXT NOT NULL, password TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS "visitor_logs" (id VARCHAR DEFAULT gen_random_uuid() NOT NULL, visitor_type TEXT DEFAULT 'unknown'::text NOT NULL, path TEXT NOT NULL, method TEXT DEFAULT 'GET'::text NOT NULL, user_agent TEXT, ip TEXT, referer TEXT, country TEXT, fingerprint TEXT, wallet_address TEXT, session_id TEXT, status_code INTEGER, created_at TIMESTAMP DEFAULT now());